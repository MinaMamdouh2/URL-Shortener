package mid

// This a containment package. BUt why? we said that we should avoid that.
// We can get away with them if the surface array of the import is going to be tiny
// and it is tiny in this package so we won't flip the pyramid here.
// We have very small functions here that it doesn't make sense to create a whole packages from middleware functions.
